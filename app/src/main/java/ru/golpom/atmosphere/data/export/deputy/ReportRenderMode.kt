/**
 * Режим HTML-отчёта: интерактивный (прокрутка) или для PDF (без горизонтального скролла).
 */
package ru.golpom.atmosphere.data.export.deputy

enum class ReportRenderMode {
    /** Просмотр в браузере — горизонтальная прокрутка длинных графиков. */
    SCREEN,

    /** Печать в PDF — агрегация по месяцам и перенос столбцов на новую строку. */
    PDF,
}
